{
  "name": "creative-coding-gallery",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-project-index.mjs",
    "dev": "nuxt dev --host",
    "prebuild": "node scripts/generate-project-index.mjs",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "validate:projects": "node scripts/validate-projects.mjs",
    "generate:projects": "node scripts/generate-project-index.mjs",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/ui": "^3.0.0",
    "@types/p5": "^1.7.7",
    "nuxt": "^4.0.0",
    "p5": "^2.2.1",
    "simplex-noise": "^4.0.3",
    "vue": "latest"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "typescript": "latest",
    "vue-tsc": "latest"
  }
}
